default.pcm.rate_converter "speexrate_best";

pcm.!spdif {
  type hw;
  card 0;
}

ctl.equal {
    type equal;
}

pcm.plugequal {
    type equal;
    slave.pcm "plug:dmix";
}

pcm.!default {
  type plug;
  # slave.pcm "dmix";
  slave.pcm plugequal;
}
